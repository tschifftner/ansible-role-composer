---
- hosts: localhost
  remote_user: root

  pre_tasks:
    - name: Install required packages for testing
      apt:
        pkg: '{{ item }}'
        state: present
        update_cache: yes
      with_items:
        - cron
        - php